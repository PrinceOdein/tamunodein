<section class="min-h-screen flex items-center justify-center px-6">
  <div class="text-center max-w-2xl fade-group">
    <h1
      class="font-bold leading-tight fade-item
             text-3xl sm:text-4xl md:text-5xl"
      style="animation-delay: 0ms"
    >
      Hi. I'm Tamunodein.
    </h1>

    <p
      class="mt-5 sm:mt-6 text-sm sm:text-base text-zinc-400 fade-item"
      style="animation-delay: 120ms"
    >
      Software Engineer • Web3 • Game Dev
    </p>

    <p
      class="mt-6 sm:mt-8 text-sm sm:text-base text-zinc-400 leading-relaxed fade-item"
      style="animation-delay: 240ms"
    >
      I build web platforms, backend systems, and Web3 projects that actually ship.
    </p>

    <div
      class="flex flex-col sm:flex-row justify-center gap-4 sm:gap-6 mt-10 sm:mt-12 fade-item"
      style="animation-delay: 360ms"
    >
      <a
        href="#projects"
        class="px-8 py-4 bg-white text-black rounded-md text-sm font-medium
               hover:scale-[1.02] transition"
      >
        View Projects
      </a>

      <a
        href="#contact"
        class="px-8 py-4 border border-zinc-600 rounded-md text-sm font-medium
               hover:border-white transition"
      >
        Contact Me
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .fade-item {
    opacity: 0;
    animation: fadeUp 0.6s ease-out forwards;
  }

  @media (prefers-reduced-motion: reduce) {
    .fade-item {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }
</style>
