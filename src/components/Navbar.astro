<nav class="fixed top-0 w-full z-50 bg-zinc-950/80 backdrop-blur border-b border-zinc-800">
  <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
    <a href="/" class="font-bold text-lg tracking-wide">ΞÐΞĮÑ†</a>

    <!-- Desktop Nav -->
    <ul class="hidden md:flex gap-6 text-sm text-zinc-300">
      <li><a href="#about" class="hover:text-white transition">About</a></li>
      <li><a href="#projects" class="hover:text-white transition">Projects</a></li>
      <li><a href="#blog" class="hover:text-white transition">Blog</a></li>
      <li><a href="#contact" class="hover:text-white transition">Contact</a></li>
    </ul>

    <!-- Hamburger -->
    <button
      id="menu-btn"
      class="md:hidden relative w-8 h-8 text-zinc-300 focus:outline-none"
      aria-label="Toggle menu"
    >
      <span class="line top"></span>
      <span class="line middle"></span>
      <span class="line bottom"></span>
    </button>
  </div>
</nav>

<!-- Mobile Menu Overlay -->
<div
  id="mobile-menu"
  class="fixed inset-0 z-40 bg-zinc-950/95 backdrop-blur-sm opacity-0 pointer-events-none transition-all duration-300"
>
  <ul
    class="flex flex-col items-center justify-center h-full gap-8 text-lg text-zinc-300"
  >
    <li><a href="#about" class="menu-link">About</a></li>
    <li><a href="#projects" class="menu-link">Projects</a></li>
    <li><a href="#blog" class="menu-link">Blog</a></li>
    <li><a href="#contact" class="menu-link">Contact</a></li>
  </ul>
</div>

<style>
  .line {
    position: absolute;
    left: 0;
    width: 100%;
    height: 2px;
    background: currentColor;
    transition: all 0.3s ease;
  }

  .top {
    top: 8px;
  }

  .middle {
    top: 15px;
  }

  .bottom {
    top: 22px;
  }

  .open .top {
    transform: rotate(45deg);
    top: 15px;
  }

  .open .middle {
    opacity: 0;
  }

  .open .bottom {
    transform: rotate(-45deg);
    top: 15px;
  }

  .menu-open {
    opacity: 1;
    pointer-events: auto;
  }

  .menu-link {
    transition: color 0.2s ease, transform 0.2s ease;
  }

  .menu-link:hover {
    color: white;
    transform: scale(1.05);
  }
</style>

<script>
  const btn = document.getElementById("menu-btn");
  const menu = document.getElementById("mobile-menu");
  const links = document.querySelectorAll(".menu-link");

  btn?.addEventListener("click", () => {
    btn.classList.toggle("open");
    menu.classList.toggle("menu-open");
  });

  links.forEach((link) =>
    link.addEventListener("click", () => {
      btn.classList.remove("open");
      menu.classList.remove("menu-open");
    })
  );
</script>
